interface:
  display_name: "Bootstrap Python Service"
  short_description: "Bootstrap uv FastAPI projects and workspaces."
  default_prompt: "Use $bootstrap-python-service to create a uv FastAPI project or workspace, optionally apply layered customization profiles, run the canonical shell entrypoint, and return the emitted run and validation commands."
