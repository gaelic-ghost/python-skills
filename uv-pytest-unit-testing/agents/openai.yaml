interface:
  display_name: "Pytest for UV Workspaces"
  short_description: "Set up and run pytest with uv."
  default_prompt: "Use $uv-pytest-unit-testing to detect repo shape, optionally apply layered customization profiles, bootstrap pytest for a uv project or workspace, keep package targeting explicit, and run tests through the canonical shell entrypoints."
