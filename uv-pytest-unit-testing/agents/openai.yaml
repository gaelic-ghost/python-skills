interface:
  display_name: "Pytest for UV Workspaces"
  short_description: "Set up and run pytest with uv"
  default_prompt: "Use $uv-pytest-unit-testing to set up and run pytest in a uv project or workspace, optionally applying layered customization profiles."
