interface:
  display_name: "Bootstrap UV Python Workspace"
  short_description: "Bootstrap uv Python projects and workspaces."
  default_prompt: "Use $bootstrap-uv-python-workspace to create a uv project or workspace with package or service profiles, optionally apply layered customization profiles, run the canonical shell entrypoint, and return the emitted validation commands."
